<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tzauto.dao.MainMapping">
    <insert id="add">
      insert  into relations (recipe_name,material_number,fixtureno,last_modify_time) value (#{recipeName}, #{materialNumber},#{fixtureno},now())
    </insert>
    <update id="update">
        update relations set  recipe_name = #{recipeName} , material_number = #{materialNumber},fixtureno=#{fixtureno},last_modify_time= now() where id = #{id}
    </update>
    <delete id="delete">
        delete from relations where id = #{id}
    </delete>

    <select id="getAll" resultType="com.tzauto.entity.RelationEntity">

    select * from  relations order by last_modify_time desc,id

    </select>
    <select id="query" resultType="com.tzauto.entity.RelationEntity">
        select * from  relations <where>
        <if test="materialNumber != null and materialNumber != ''">
            and material_number = #{materialNumber}
        </if>
        <if test="fixtureno != null and fixtureno != ''">
            and fixtureno = #{fixtureno}
        </if>

    </where>


    </select>
    <select id="queryUser" resultType="java.lang.String">
 select login_name from  sys_user where login_name=#{name} and
 password = #{passWord}

    </select>

</mapper>
